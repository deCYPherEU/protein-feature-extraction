name: MSA component
description: This component is responsible for handling the MSA sequence.
image: msa_component:latest

consumes:
  sequence:
        type: string
  sequence_checksum:
        type: string

produces:
  sequence:
        type: string
  sequence_checksum:
        type: string
  msa_sequence:
        type: string
